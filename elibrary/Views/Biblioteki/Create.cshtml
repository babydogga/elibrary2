@model Biblioteka

@{
    ViewData["Title"] = "Dodaj nową bibliotekę";
}

<div class="row text">
    <div class="col-md-8 offset-2">
        <p>
            <h1>Dodaj nową bibliotekę</h1>
        </p>
        <div class="row">
            <div class="col-md offset-2">
                <form asp-action="Create">
                    <div asp-validation-summary="ModelOnly" class="text-danger"></div>
                    <div class="form-group text-center">
                        <img id="ProfilePicturePreview" />
                    </div>
                    <div class="form-group mb-3">
                        <label asp-for="LogoBib" class="conrol-label"></label>
                        <input asp-for="LogoBib" class="form-control" />
                        <span asp-validation-for="LogoBib" class="text-danger"></span>
                    </div>
                    <div class="form-group mb-3">
                        <label asp-for="NameBib" class="conrol-label"></label>
                        <input asp-for="NameBib" class="form-control" />
                        <span asp-validation-for="NameBib" class="text-danger"></span>
                    </div>
                    <div class="form-group mb-3">
                        <label asp-for="DescBib" class="conrol-label"></label>
                        <input asp-for="DescBib" class="form-control" />
                        <span asp-validation-for="DescBib" class="text-danger"></span>
                    </div>

                    <div class="d-flex justify-content-between">
                        <input type="submit" value="Create" class="btn btn-primary" />
                        <a class="btn btn-outline-secondary" asp-action="Index">Show All</a>
                    </div>
                </form>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    <script>
        $(document).ready(function () {
            var output = document.getElementById('ProfilePicturePreview');
            var profilePictureURL = $("#LogoBib").val(); // Pobranie wartości poprawnie
            if (profilePictureURL) {
                output.src = profilePictureURL;
            }
        });

        $("#LogoBib").on("input", function () { // Użycie "input" zamiast "change" dla lepszej reakcji
            var output = document.getElementById('ProfilePicturePreview');
            output.src = $(this).val();
        });
    </script>
}
